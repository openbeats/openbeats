import {} from "../types";

const initialState = {}

export const addSearchTag = (state = initialState, action) => {
    switch (action.type) {
        default:
            break;
    }
    return state;
}